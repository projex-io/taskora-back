CREATE TABLE tickets (
                         id BIGSERIAL PRIMARY KEY,
                         title VARCHAR(255),
                         description TEXT,
                         status VARCHAR(50)
);
