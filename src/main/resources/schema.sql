CREATE TABLE tickets (
                         id SERIAL PRIMARY KEY,
                         title VARCHAR(255),
                         description TEXT,
                         status VARCHAR(50)
);
