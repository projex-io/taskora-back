name: Publish Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  publish_release:
    uses: projex-io/workflows-kt/.github/workflows/release.yml@main