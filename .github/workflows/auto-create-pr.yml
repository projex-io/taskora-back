name: Auto-create PR on push branch

on:
  create:
    branches:
      - '*'

jobs:
  auto_pr:
    uses: projex-io/workflows-kt/.github/workflows/auto-create-pr.yml@main
    secrets:
      PAT_TOKEN: ${{ secrets.PAT_TOKEN }}