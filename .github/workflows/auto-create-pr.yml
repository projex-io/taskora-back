name: Auto-create PR on push branch

on:
  create:
    branches:
      - '*'

jobs:
  auto_pr:
    uses: aassalax/workflows/.github/worflows/kt/auto-create-pr.yml@main
    secrets:
      PAT_TOKEN: ${{ secrets.PAT_TOKEN }}